// Layout.tsx
import React from 'react'; // Importing React makes the file a module
export {};
const Layout = () => {
  return (
    <div>
      {/* Your layout content */}
    </div>
  );
};

export default Layout;
